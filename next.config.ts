import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['plus.unsplash.com', 'images.unsplash.com', 'example.com'], // Add your image domains here
  },
};

export default nextConfig;
